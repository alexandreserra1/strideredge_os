# Python
__pycache__/
*.pyc
.venv/
*.egg-info/
.pytest_cache/

# Rust (artefatos de build)
target/
rust_kernel/target/

# Extensão compilada (gerada pelo maturin)
*.so

# Bancos DuckDB (gerados — não versionar)
storage/*.db
storage/*.wal

# Dados pessoais de telemetria (.FIT) — privacidade / LGPD
*.fit
*.FIT
storage/samples/

# Videos enviados pelo usuario (analise de forma) — grandes + pessoais, nao versionar
storage/videos/

# Pesos de modelo (ONNX/YOLO) — grandes, baixados via script, nao versionar
stride_vision/models/

# Conjunto-ouro rotulado a mao p/ calibrar o LLM-judge — referencia activity_id local, nao portavel
storage/coach_gold.json

# Segredos / credenciais (NUNCA versionar)
.env

# Logs (gerados)
*.log

# macOS
.DS_Store
storage/.auth_secret

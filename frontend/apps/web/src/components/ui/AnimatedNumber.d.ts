/** Conta de 0 até `value` com easing — micro-interação sóbria (sem infantilizar). */
export default function AnimatedNumber({ value, decimals, duration, className, }: {
    value: number;
    decimals?: number;
    duration?: number;
    className?: string;
}): import("react").JSX.Element;
//# sourceMappingURL=AnimatedNumber.d.ts.map